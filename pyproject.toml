[project]
name = "topaz-auto-enhance"
version = "0.1.0"
description = "Automation CLI for Topaz Video enhancement and frame interpolation"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "HopemanW"}]

[project.scripts]
topaz-auto = "topaz_auto:main"

[tool.ruff]
line-length = 100
target-version = "py310"
